# Para probar la app en tu PC antes de compilar el APK:
#   pip install -r requirements.txt
#   python main.py
#
# Nota: buildozer.spec ya trae su propia lista de "requirements" para el
# build de Android; este archivo es solo para desarrollo/prueba en escritorio.

kivy>=2.3.0
plyer>=2.1.0
pypdf>=5.0.0
python-docx>=1.1.2
fpdf2>=2.8.0
